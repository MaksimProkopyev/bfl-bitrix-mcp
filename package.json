{
  "name": "bfl-bitrix-mcp",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:schemas": "npx --yes -p ajv-cli@5 -p ajv-formats ajv compile -c ajv-formats -s schemas/query_watcher_output.json -s schemas/batch_candidates.json -s schemas/scrubber_output.json",
    "ci": "npm run validate:schemas"
  }
}
